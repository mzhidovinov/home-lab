# Copy to values-secret-store-vault.yaml (gitignored) for local secret overrides.
# Actual secrets belong here or in values-secret.yaml — never commit the copy.
#
# See: https://validatedpatterns.io/learn/secrets-management-in-the-validated-patterns-framework/
#
# Example (do not commit real values):
# secrets:
#   - name: my-app-creds
#     fields:
#       - name: password
#         value: "replace-me"

secrets: []
